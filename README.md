# 🖼️ Google Image Scraper with `google_images_search`

This project allows you to download images from Google Search using the `google_images_search` Python library. It uses a Google Custom Search Engine (CSE) and an API key to fetch and download images based on a given keyword.

---

## 📦 Features

- Automatically downloads images from Google based on keywords.
- Stores them in a local folder.
- Option to zip the downloaded images.
- Easily customizable for various search parameters (e.g., image size, type, color, rights).

---

## 🔧 Requirements

- Python 3.6+
- A Google API Key
- A Google Custom Search Engine (CSE ID)

Install dependencies using pip:

```bash
pip install google_images_search
